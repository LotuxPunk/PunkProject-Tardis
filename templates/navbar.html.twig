<!-- Image and text -->
<nav class="navbar navbar-dark navbar-expand-lg sticky-top bg-dark">
    <a class="navbar-brand" href="/">
        <img src="{{asset('images/punkproject.png')}}" width="30" height="30" class="d-inline-block align-top rounded" alt="" loading="lazy"> PunkProject
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="/">Home</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('asset_index')}}">Assets</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('user_index')}}">Users</a>
            </li>
            {% if is_granted('ROLE_USER') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{path('dashboard')}}">Dashboard</a>
                </li>
            {% endif %}
        </ul>
        {% if not app.user %}
            <span class="navbar-text">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('login')}}">Login</a>
                    </li>
                </ul>
            </span>
        {% else %}
            <span class="navbar-text">
                <img src="https://cdn.discordapp.com/avatars/{{app.user.discordId}}/{{app.user.avatar}}.png" width="30" height="30" class="d-inline-block align-top rounded-circle" alt="" loading="lazy"> 
                <a href="{{ path('user_show', {'id': app.user.id}) }}">{{app.user.username}}</a>
                &nbsp;
                <a href="{{path('app_logout')}}" class="btn btn-danger text-center align-middle">
                    <svg width="1em" height="1em" viewBox="0 0 16 16" class="bi bi-door-closed" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
                        <path fill-rule="evenodd" d="M3 2a1 1 0 0 1 1-1h8a1 1 0 0 1 1 1v13a1 1 0 0 1-1 1H4a1 1 0 0 1-1-1V2zm1 0v13h8V2H4z"/>
                        <path d="M7 9a1 1 0 1 1-2 0 1 1 0 0 1 2 0z"/>
                        <path fill-rule="evenodd" d="M1 15.5a.5.5 0 0 1 .5-.5h13a.5.5 0 0 1 0 1h-13a.5.5 0 0 1-.5-.5z"/>
                    </svg>
                </a>
            </span>
        {% endif %}
    </div>
</nav>