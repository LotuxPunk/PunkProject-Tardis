<!-- Image and text -->
<nav class="navbar navbar-dark navbar-expand-lg sticky-top bg-dark">
    <a class="navbar-brand" href="/">
        <img src="{{asset('images/punkproject.png')}}" width="30" height="30" class="d-inline-block align-top" alt="" loading="lazy"> PunkProject
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="/">Home</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('asset_index')}}">Assets</a>
            </li>
            {% if is_granted('ROLE_USER') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{path('dashboard')}}">Dashboard</a>
                </li>
            {% endif %}
        </ul>
        {% if not app.user %}
            <span class="navbar-text">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('login')}}">Login</a>
                    </li>
                </ul>
            </span>
        {% else %}
            <span class="navbar-text">
                <img src="https://cdn.discordapp.com/avatars/{{app.user.discordId}}/{{app.user.avatar}}.png" width="30" height="30" class="d-inline-block align-top" alt="" loading="lazy"> {{app.user.username}}
            </span>
        {% endif %}
    </div>
</nav>