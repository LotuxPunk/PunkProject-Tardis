<nav class="navbar navbar-expand-lg navbar-dark bg-secondary sticky-top">

    <a class="navbar-brand" href="{{path('home')}}">
        <img src="{{asset('images/punkproject.png')}}" class="rounded d-inline-block align-top" width="30" height="30" alt="PunkProject's logo">
        PunkProject
    </a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Dashboard
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="#">Link</a>
                    </div>
                    
                </li>
            {% endif %}
            {% if not app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{path('registration')}}">Register</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('connect')}}">Login</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{path('disconnect')}}">Logout</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>